# 있니(Inni) 기능명세서

| 항목 | 내용 |
|---|---|
| 문서 버전 | 1.0 |
| 작성일 | 2026-09-16 |
| 기준 | 현재 작업 폴더의 구현 코드, 기준 커밋 `7e6c2ae` |
| 서비스 목적 | 옷을 구매하기 전에 비슷한 보유 의류를 확인하고 구매 여부를 기록한다. |
| 핵심 흐름 | 회원가입·로그인 → 구매 후보 등록 → 보유 의류 비교 → 샀다·안 샀다 결정 → 기록 관리 |
| 확인 범위 | 화면·API·데이터 모델의 소스 검토. 운영 배포 및 실제 DB 동작을 이번 문서 작성에서 재검증한 것은 아니다. |

기존 `PRD.md`에는 초기 프론트엔드 MVP와 향후 계획이 함께 남아 있다. 이 문서는 현재 코드의 동작을 기준으로 하며, **화면 제공**, **API만 제공**, **미구현**을 구분한다. 기능 ID는 이 문서의 관리용 식별자다.

## 1. 사용자와 이용 범위

| 사용자 | 이용 가능한 기능 |
|---|---|
| 비로그인 방문자 | 서비스 소개, 로그인, 회원가입 |
| 로그인 사용자 | 본인의 구매 후보 등록·조회·삭제, 구매 여부 기록, 보유 의류 등록·조회·수정·삭제, 로그아웃 |
| 인증된 API 사용자 | 화면 기능 외 구매 후보 정보 수정·일부 상태 복원, 본인 계정 삭제 |

사용자별 구매 후보와 보유 의류는 서버에서 로그인 사용자 ID로 구분한다. 다른 사용자의 개별 데이터에 접근하면 존재하지 않는 데이터와 동일하게 404를 반환한다. 별도 관리자 화면과 관리자 권한은 구현되어 있지 않다.

## 2. 화면 구성

| 화면 ID | 화면 | 경로 | 접근 조건 | 주요 기능 |
|---|---|---|---|---|
| SC-01 | 서비스 소개 | `/` | 비로그인 | 서비스 설명, 사용 흐름, 가입 유도 |
| SC-02 | 기존 소개 시안 | `/v1` | 공개 | 백업 랜딩 페이지 |
| SC-03 | 로그인·회원가입 | `/login` | 공개 | 이메일·비밀번호 인증, 모드 전환 |
| SC-04 | 살까 목록 | `/` | 로그인 | 상태 탭, 카테고리 필터, 후보 카드, 후보 추가 |
| SC-05 | 구매 후보 작성 | `/wants/new` | 로그인 | 기본·선택 정보 입력, 비슷한 보유 의류 미리보기 |
| SC-06 | 구매 후보 상세 | `/wants/:id` | 로그인 | 상세 정보, 겹침 확인, 보유 한 줄 추가, 구매 결정, 삭제 |
| SC-07 | 집에 있는 것 | `/owns` | 로그인 | 보유 목록, 카테고리·색 필터, 수정·삭제 대화상자 |

- 소개 화면의 가입 CTA는 `/login?mode=signup`으로 이동한다.
- 비로그인 상태로 보호 화면에 접근하면 `/login`으로 이동한다.
- 로그인 상태에서 `/login`에 접근하면 `/`으로 이동한다.
- 정의되지 않은 화면 경로는 `/`으로 이동한다.
- 구매 후보 저장 성공 후 해당 후보 상세로 이동한다. 구매 결정 또는 후보 삭제 성공 후 홈으로 이동한다.

## 3. 기능 상세

### 3.1 회원 및 인증

| ID | 기능 | 입력·실행 조건 | 처리 및 결과 | 예외·제한 | 제공 상태 |
|---|---|---|---|---|---|
| AUTH-01 | 회원가입 | 이메일, 비밀번호 입력 후 회원가입 | 이메일 앞뒤 공백 제거·소문자 변환, 비밀번호 해시 저장, 토큰 발급 후 자동 로그인 | 이메일 중복 409, 형식 오류 400 | 화면 제공 |
| AUTH-02 | 로그인 | 이메일, 비밀번호 입력 후 로그인 | 계정 확인 후 사용자 정보와 토큰 발급, 홈 표시 | 잘못된 이메일 또는 비밀번호 401 | 화면 제공 |
| AUTH-03 | 로그인 복원 | 브라우저에 토큰이 남은 상태에서 앱 진입 | 현재 사용자 조회 후 로그인 상태 복원 | 인증 API가 401을 반환하면 토큰 제거·비로그인 처리 | 자동 처리 |
| AUTH-04 | 로그아웃 | 공통 헤더의 로그아웃 선택 | 현재 브라우저 토큰과 로그인 상태 제거, 보호 데이터 표시 종료 | 서버 토큰 폐기 API는 없음 | 화면 제공 |
| AUTH-05 | 회원 탈퇴 | 유효한 인증 토큰으로 계정 삭제 요청 | 사용자 및 연결된 구매 후보·보유 의류 삭제 | 화면 버튼·탈퇴 확인 화면 없음 | API만 제공 |

**입력 및 인증 규칙**

- 이메일: 올바른 이메일 형식, 최대 320자. 대소문자를 구분하지 않고 저장·조회한다.
- 가입 비밀번호: 최소 8자, UTF-8 기준 최대 72바이트. 로그인 요청은 비어 있지 않아야 하며 최대 72바이트다.
- 비밀번호는 bcrypt 해시로 저장하고 응답에 포함하지 않는다.
- JWT 유효기간은 7일이다. 브라우저에는 인증 토큰을 저장하며 의류 데이터는 서버 DB에 저장한다.
- 회원가입과 로그인 요청 제한은 각각 IP별 15분에 5회, 10회다. 초과 시 429와 재시도 대기 정보를 반환한다. 현재 제한 카운터는 서버 인스턴스 메모리 기준이다.
- 별도 토큰 갱신, 소셜 로그인, 이메일 인증, 비밀번호 찾기·변경 화면은 없다.

### 3.2 구매 후보 관리

| ID | 기능 | 입력·실행 조건 | 처리 및 결과 | 예외·제한 | 제공 상태 |
|---|---|---|---|---|---|
| WANT-01 | 후보 등록 | 이름·카테고리·색 입력 후 ‘넣기’ | `pending` 상태로 생성하고 상세 화면 이동 | 기타 선택 시 상세값 필수. 필수 입력 오류는 저장 차단 | 화면 제공 |
| WANT-02 | 후보 목록 | 홈 진입 | 기본 ‘진행 중’ 목록, 최신 생성순 표시 | 빈 목록·조회 실패·로딩 상태 구분 | 화면 제공 |
| WANT-03 | 상태·카테고리 필터 | 진행 중·샀다·안 샀다 탭 및 카테고리 선택 | 두 조건을 함께 만족하는 후보 표시 | 필터 결과가 없으면 초기화 제공 | 화면 제공 |
| WANT-04 | 후보 상세 | 후보 카드 선택 또는 상세 경로 접근 | 이름, 분류, 색, 상태, 입력된 가격·링크·메모 표시 | 데이터가 없으면 ‘없는 후보입니다’ 및 홈 이동 제공 | 화면 제공 |
| WANT-05 | 외부 링크 열기 | 링크가 있는 후보 상세에서 선택 | 새 탭으로 저장된 주소 열기 | 링크 미입력 시 표시하지 않음 | 화면 제공 |
| WANT-06 | 후보 정보 수정 | 후보 ID와 변경 필드 전송 | 이름·분류·색·가격·링크·메모 수정 | 전용 편집 화면 없음. 연결된 보유 의류는 자동 수정하지 않음 | API만 제공 |
| WANT-07 | 후보 삭제 | 상세에서 삭제 선택 후 확인 | 해당 후보 삭제 후 홈 이동 | 구매 완료 후보는 삭제 차단(409), 내 옷장으로 이동 안내 | 화면 제공 |

목록 카드는 후보 이름, 입력된 가격, 상태를 보여 준다. 비슷한 보유 의류가 있으면 ‘집에 있음’을 표시하고, 없으면 카테고리를 표시한다. 목록 API는 커서 페이지네이션을 제공한다(기본 50개, 최대 100개). 현재 화면은 필터와 겹침 판단을 위해 페이지를 자동으로 이어 받아 전체 목록을 구성한다. 검색어 검색·더 보기 버튼·정렬 선택은 제공하지 않는다.

### 3.3 비슷한 보유 의류 확인

| ID | 기능 | 실행 조건 | 처리 및 결과 |
|---|---|---|---|
| MATCH-01 | 작성 중 비교 | 후보 작성 중 카테고리·색과 필요한 기타 상세값 입력 | 현재 보유 목록과 즉시 비교하여 일치 개수와 이름 표시 |
| MATCH-02 | 상세 비교 | 후보 상세 조회 | ‘집에 비슷한 옷 N개’ 및 해당 의류 이름 표시 |
| MATCH-03 | 목록 요약 | 구매 후보 목록 조회 | 일치하는 보유 의류가 1개 이상이면 ‘집에 있음’ 표시 |

**일치 판정 규칙**

1. 카테고리와 색 코드가 모두 같아야 한다.
2. 카테고리가 기타(`etc`)이면 종류 상세값도 같아야 한다. 비교 전에 앞뒤·내부 공백을 제거하고 영문을 소문자로 바꾼다.
3. 색이 기타(`other`)이면 색 상세값도 같아야 한다. 위 정규화에 더해 마지막 ‘색’ 한 글자를 제거한다.
4. 기타 상세값이 비어 있으면 일치로 처리하지 않는다.
5. 이름·가격·브랜드·이미지는 판정에 사용하지 않는다. 이미지 인식이나 AI 추천 기능이 아니다.

| 후보 / 보유 예시 | 판정 |
|---|---|
| 상의·블랙 / 상의·블랙 | 일치 |
| 상의·블랙 / 아우터·블랙 | 불일치 |
| 기타 종류 ‘버킷 햇’ / ‘버킷햇’, 색 동일 | 일치 |
| 기타 색 ‘카키색’ / ‘카키’, 카테고리 동일 | 일치 |
| 기타 색 ‘검정’ / 기본 색 블랙 | 불일치: 색 코드가 다름 |

일치 결과가 0개이면 안내 상자를 표시하지 않는다. 구매 완료로 생성된 보유 의류도 비교 대상에 포함되므로, 구매 완료 후보가 자신으로부터 생성된 보유 의류와 일치할 수 있다.

### 3.4 구매 결정 및 상태 변경

| ID | 기능 | 실행 조건 | 처리 및 결과 | 예외·제한 |
|---|---|---|---|---|
| DEC-01 | 샀다 | 진행 중 후보에서 ‘샀다’ 선택 | 후보를 `bought`로 변경하고 같은 정보를 가진 보유 의류 1건 생성 | 두 작업은 트랜잭션으로 처리. 실패 시 함께 취소 |
| DEC-02 | 안 샀다 | 진행 중 후보에서 ‘안 샀다’ 선택 | 후보를 `skipped`로 변경하고 홈 이동 | 보유 의류를 생성하지 않음 |
| DEC-03 | 상태 복원 | API에서 `skipped` 후보에 `pending` 요청 | 다시 진행 중으로 변경 | 화면에 되돌리기 버튼 없음 |

| 현재 상태 | 요청 | 결과 |
|---|---|---|
| `pending` | 구매 완료 API | `bought` + 보유 생성 |
| `pending` | 상태 수정 `skipped` | `skipped` |
| `skipped` | 상태 수정 `pending` | `pending`, API만 제공 |
| `skipped` | 구매 완료 API 직접 호출 | 409 `WANT_NOT_PENDING` |
| `bought` | 구매 완료 API 재호출 | 연결 보유가 있으면 200과 기존 후보·보유 반환(created: false), 추가 생성 없음 |
| `bought` | 상태 수정 요청 | 409 `WANT_ALREADY_BOUGHT` |

화면의 ‘샀다’·‘안 샀다’·‘집에 이런 거 있음’은 진행 중 후보에서만 표시한다. `bought` 상태는 일반 수정 API로 지정할 수 없고 구매 완료 API를 사용해야 한다. 구매 완료 상태인데 연결 보유가 없는 경우 구매 완료 재호출은 409다.

### 3.5 보유 의류 관리

| ID | 기능 | 입력·실행 조건 | 처리 및 결과 | 예외·제한 |
|---|---|---|---|---|
| OWN-01 | 한 줄 등록 | 진행 중 후보 상세에서 보유 이름 입력 후 ‘한 줄 추가’ | 후보의 카테고리·색·기타 상세를 복사해 수동 보유 생성, 비교 결과 갱신 | 이름 필수. 후보의 가격·링크·메모는 복사하지 않음 |
| OWN-02 | 구매 시 자동 등록 | 후보의 ‘샀다’ 처리 성공 | 이름·분류·색·가격·링크·메모를 복사하고 원본 후보 연결 | 생성 시점의 복사본이며 이후 양쪽 정보는 자동 동기화하지 않음 |
| OWN-03 | 목록 조회 | ‘보유’ 이동 | 최신 생성순으로 이름·카테고리·색 표현·등록 출처 표시 | 목록 카드에는 가격·링크·메모를 표시하지 않음 |
| OWN-04 | 필터 | 카테고리·색 선택 | 두 조건을 만족하는 보유 의류 표시 | 결과 없음과 전체 보유 없음 구분, 필터 초기화 제공 |
| OWN-05 | 정보 수정 | 보유 카드 관리 대화상자 열기 | 이름·분류·색·가격·링크·메모 수정 후 저장 | 빈 선택 정보는 삭제 가능. 필수값 오류 시 저장 차단 |
| OWN-06 | 삭제 | 관리 대화상자에서 삭제 선택 후 확인 | 보유 삭제. 연결된 구매 후보가 있으면 해당 구매 기록도 함께 삭제 | 삭제 전 연결 기록 동시 삭제 안내, 트랜잭션 처리 |

관리 대화상자는 카드 500ms 길게 누르기, 우클릭, 키보드 Enter·Space로 연다. 보유 목록 자체에는 신규 등록 버튼이 없다. 전체 필드를 입력하는 수동 보유 등록은 API로 가능하며, 화면에서는 후보 상세의 한 줄 등록을 사용한다.

**삭제를 시작한 화면에 따른 차이**

구매 완료 후보는 직접 삭제할 수 없다. 상세 화면은 삭제 대신 내 옷장 이동을 안내하고, API도 409로 거절한다. `집에 있는 것`에서 구매로 생성된 보유 의류를 삭제하면 연결된 구매 기록도 함께 삭제한다. 진행 중·안 샀다 후보는 직접 삭제할 수 있다.

| 삭제를 시작한 위치 | 삭제되는 데이터 | 유지되는 데이터 |
|---|---|---|
| 후보 상세에서 진행 중·안 샀다 후보 삭제 | 구매 후보 | 직접 등록한 보유 의류 |
| 구매 완료 후보 삭제 요청 | 없음(409) | 구매 후보와 연결된 보유 의류 모두 유지 |
| `집에 있는 것`에서 구매로 생성된 보유 의류 삭제 | 보유 의류 + 연결된 구매 후보 | 그 외 기록 |
| `집에 있는 것`에서 직접 적은 보유 의류 삭제 | 해당 보유 의류 | 다른 후보·보유 기록 |
| 계정 삭제 API 호출 | 사용자 + 본인 후보 전체 + 본인 보유 전체 | 다른 사용자 데이터 |

## 4. 입력 항목 및 검증

아래 길이·형식 제한은 서버 검증 기준이다. 화면은 주요 필수값·가격을 우선 검증하며, 나머지 오류는 API 응답으로 안내한다.

| 항목 | 데이터 키 | 필수 여부 | 규칙 |
|---|---|---|---|
| 이름 | `title` | 필수 | 앞뒤 공백 제거 후 1~120자 |
| 카테고리 | `category` | 필수 | 상의 `top`, 하의 `bottom`, 아우터 `outer`, 신발 `shoes`, 가방 `bag`, 기타 `etc` |
| 기타 종류 | `categoryDetail` | 조건부 필수 | 기타 선택 시 1~80자. 일반 카테고리로 변경하면 null 처리 |
| 색 | `color` | 필수 | 블랙 `black`, 화이트 `white`, 그레이 `gray`, 네이비 `navy`, 베이지 `beige`, 브라운 `brown`, 기타 `other` |
| 기타 색 이름 | `colorDetail` | 조건부 필수 | 기타 선택 시 1~80자. 일반 색으로 변경하면 null 처리 |
| 링크 | `url` | 선택 | 유효한 URL, 최대 2,048자 |
| 가격 | `price` | 선택 | 0~2,147,483,647 사이 정수. 화면 표시는 원 단위. 미입력과 0원 구분 |
| 메모 | `note` | 선택 | 앞뒤 공백 제거, 최대 2,000자 |

- 선택 정보 없이 이름·카테고리·색만으로 후보를 등록할 수 있다.
- 보유 수정에서 가격·링크·메모를 지우면 null로 저장한다.
- API 수정 요청에서 생략한 필드는 기존 값을 유지한다. 빈 수정 요청과 정의되지 않은 필드는 검증 오류다.
- 생성 요청에서 사용자 ID·출처·구매 상태를 임의 지정할 수 없다. 서버가 인증 및 생성 경로에 따라 결정한다.
- 개별 리소스 ID는 UUID 형식이어야 한다.

## 5. 공통 화면 상태 및 오류 처리

| 상황 | 동작 |
|---|---|
| 데이터 불러오는 중 | 로딩 상태 표시, 조회 완료 전 목록 대신 로딩 UI 제공 |
| 초기 조회 실패 | 오류 안내와 다시 시도 버튼 제공 |
| 최초 진행 중 후보 없음 | 후보 등록을 유도하는 빈 화면 표시 |
| 보유 의류 없음 | 보유 등록 방법을 설명하는 빈 화면 표시 |
| 필터 결과 없음 | 결과 없음 안내와 필터 초기화 제공 |
| 등록·수정·구매 처리 중 | 해당 주요 실행 버튼 비활성화 및 스피너 표시 |
| 입력 오류 | 화면에서 검증한 항목은 필드 오류 표시. 저장 API 오류는 토스트로 안내 |
| 서버 연결 실패 | ‘서버에 연결하지 못했습니다. 잠시 후 다시 시도해 주세요.’ 안내 |
| 인증 만료·무효 | 인증 요청 401 시 토큰 제거, 보호 화면은 로그인으로 이동 |
| 저장 성공 | 서버 응답을 받은 뒤 목록 상태 갱신 |
| 삭제 취소 | 삭제 요청 없이 기존 데이터 유지 |

로그인 오류는 폼 안내 영역에 표시한다. 자동 로그인 복원 중 서버 오류가 발생하면 사용자 상태를 비로그인으로 처리하며, 인증 실패(401)와 달리 모든 오류에서 토큰이 제거되는 것은 아니다.

## 6. API 명세 요약

인증 필요 API는 `Authorization: Bearer <JWT>`를 사용한다. 요청·응답 상세 스키마는 `server/openapi.js`와 실제 라우트 코드를 함께 참고한다.

| Method | 경로 | 인증 | 설명 | 정상 응답 |
|---|---|---|---|---|
| POST | `/api/auth/signup` | 불필요 | 회원가입 | 201 `{ user, token }` |
| POST | `/api/auth/login` | 불필요 | 로그인 | 200 `{ user, token }` |
| GET | `/api/auth/me` | 필요 | 현재 사용자 조회 | 200 `{ user }` |
| DELETE | `/api/auth/me` | 필요 | 본인 계정 삭제 | 204 |
| GET | `/api/wants` | 필요 | 후보 목록, `status`·`category` 필터 | 200 `{ wants, nextCursor }` |
| POST | `/api/wants` | 필요 | 후보 생성 | 201 `{ want }` |
| GET | `/api/wants/:id` | 필요 | 후보 단건 조회 | 200 `{ want }` |
| PATCH | `/api/wants/:id` | 필요 | 후보 정보·허용 상태 수정 | 200 `{ want }` |
| DELETE | `/api/wants/:id` | 필요 | 미구매 후보 삭제 | 204, 구매 완료는 409 |
| POST | `/api/wants/:id/buy` | 필요 | 구매 완료 및 보유 생성 | 최초 201 + Location, 재요청 200 `{ want, own, created }` |
| GET | `/api/owns` | 필요 | 보유 목록, `category`·`color` 필터 | 200 `{ owns, nextCursor }` |
| POST | `/api/owns` | 필요 | 수동 보유 생성 | 201 `{ own }` |
| GET | `/api/owns/:id` | 필요 | 본인 보유 의류 상세 | 200 `{ own }`, 없으면 404 |
| PATCH | `/api/owns/:id` | 필요 | 보유 수정 | 200 `{ own }` |
| DELETE | `/api/owns/:id` | 필요 | 보유 및 연결 후보 삭제 | 204 |
| GET | `/api/health` | 불필요 | API·DB 연결 확인 | 정상 200, DB 연결 실패 503 |
| GET | `/api/openapi.json` | 불필요 | API 정의 조회 | 200 |
| GET | `/api/docs` | 불필요 | Swagger 문서 조회 | 200 HTML |

화면은 로그인 후 후보·보유 목록을 함께 불러와 메모리에 보관하고 필터링한다. 후보 상세도 불러온 목록에서 조회한다. 단건 조회 API와 서버 필터는 별도로 제공된다. 다른 기기에서 변경한 내용을 실시간으로 수신하는 기능은 없다.

| 오류 상태 | 의미 |
|---|---|
| 400 | 입력 형식·조건·ID·필터 오류 |
| 401 | 인증 필요, 무효 토큰, 잘못된 로그인 정보 |
| 404 | 데이터 없음 또는 다른 사용자의 데이터 접근 |
| 409 | 이메일 중복, 허용되지 않는 구매 상태 변경 |
| 429 | 회원가입·로그인 요청 횟수 초과 |
| 500 | 서버 내부 오류 또는 인증 설정 오류 |
| 503 | 상태 확인 API에서 DB 연결 실패 |

일반 오류 응답은 `{ error: { code, message, fields? } }` 형태다. 상태 확인 API는 별도로 `{ status, database }`를 반환한다.

## 7. 데이터 구조

| 데이터 | 주요 필드 | 관계·정책 |
|---|---|---|
| 사용자 `User` | ID, 이메일, 비밀번호 해시, 생성·수정일 | 이메일 고유값. 사용자 1명에 후보·보유 여러 건 |
| 구매 후보 `Want` | ID, 사용자 ID, 의류 입력 항목, 상태, 생성·수정일 | 기본 상태 `pending`. 사용자 삭제 시 함께 삭제 |
| 보유 의류 `Own` | ID, 사용자 ID, 의류 입력 항목, 출처, 원본 후보 ID, 생성·수정일 | 출처 `manual`·`bought`. 원본 후보 연결은 선택이며 고유값 |

구매 완료 시 하나의 후보에 하나의 보유만 연결한다. API에서는 상태·출처를 소문자로 전달하고 DB에서는 enum으로 저장한다. 카테고리·색상도 DB enum으로 제한하며 기존 소문자 값을 그대로 사용한다. 후보·보유 정보는 서버 DB에 저장되므로 재로그인 후 다시 조회할 수 있다. 과거 실습용 브라우저 저장 데이터는 앱 시작 시 정리되며 서버로 이관하는 기능은 없다.

## 8. 구현 범위 구분

| 구분 | 항목 |
|---|---|
| 현재 화면에서 제공 | 이메일 가입·로그인·로그아웃, 후보 등록·목록·상세·삭제, 겹침 확인, 샀다·안 샀다, 보유 한 줄 등록·목록·수정·삭제 |
| API만 제공 | 계정 삭제, 후보 정보 편집, 안 샀다 후보의 진행 중 복원, 전체 필드를 직접 입력하는 보유 생성 |
| 향후 계획 또는 미구현 | Pro 결제·구독, 무료 체험 권한, 진행 중 후보 5개 제한, 절약 금액·통계, 24시간 대기, 이미지 업로드, 쇼핑몰 연동·확장 프로그램, AI 코디 |

랜딩이나 기존 기획서에 요금·Pro 관련 설명이 있어도 실제 결제·권한 제어가 구현되었다는 의미는 아니다. 현재 코드에는 플랜별 이용 제한이 없다.

## 9. 인수 확인 시나리오

아래 표는 명세를 확인하기 위한 기대 결과이며, 이번 작성 작업에서 실제 실행한 테스트 결과는 아니다.

| ID | 확인 절차 | 기대 결과 |
|---|---|---|
| AC-01 | 비로그인 상태로 `/owns` 접근 | 로그인 화면 이동 |
| AC-02 | 정상 이메일·비밀번호로 가입 후 새로고침 | 자동 로그인, 현재 사용자 복원 |
| AC-03 | 같은 이메일을 대소문자만 바꿔 재가입 | 중복 가입 차단 |
| AC-04 | 이름·카테고리·색만으로 후보 등록 | 가격 없이 저장되고 상세 화면 이동 |
| AC-05 | 기타 카테고리 또는 색을 선택하고 상세값 비우기 | 저장 차단 및 오류 안내 |
| AC-06 | 가격을 0, 음수, 소수, 최댓값 초과로 각각 입력 | 0 허용, 나머지 차단 |
| AC-07 | 상의·블랙 후보 상세에서 보유 한 줄 등록 | 같은 분류·색의 수동 보유 생성, 겹침 표시 갱신 |
| AC-08 | 기타 색 ‘카키색’ 후보와 ‘카키’ 보유 비교 | 카테고리 조건도 같으면 일치 |
| AC-09 | 가격·링크·메모가 있는 후보를 ‘샀다’ 처리 | 후보 상태 변경, 선택 정보까지 복사된 보유 1건 생성 |
| AC-10 | 동일 구매 완료 API 재요청 | 기존 보유 반환, 중복 보유 없음 |
| AC-11 | 진행 중 후보를 ‘안 샀다’ 처리 | 안 샀다 탭에서 조회, 보유 추가 없음 |
| AC-12 | 구매 완료 후보 삭제 | 409, 후보와 보유 유지 및 내 옷장 삭제 안내 |
| AC-13 | 구매 후보에 연결된 보유 삭제 | 보유와 해당 구매 후보 모두 삭제 |
| AC-14 | 보유 편집에서 가격·링크·메모 입력 후 다시 비우기 | 저장 및 선택값 삭제 정상 처리 |
| AC-15 | 다른 사용자 토큰으로 후보·보유 수정·삭제 요청 | 404, 원래 데이터 유지 |
| AC-16 | 조회 중 네트워크 실패 후 복구·다시 시도 | 오류 안내 후 목록 재조회 |
| AC-17 | 유효하지 않은 토큰으로 보호 API 요청 | 401 및 클라이언트 인증 해제 |
| AC-18 | API로 본인 계정 삭제 | 사용자와 본인 후보·보유 삭제 |

## 10. 구현 근거

| 영역 | 확인한 파일 |
|---|---|
| 라우트·접근 제어 | `src/App.jsx`, `src/components/AuthGate.jsx` |
| 인증 화면·세션 | `src/pages/LoginPage.jsx`, `src/hooks/useSession.jsx`, `src/lib/api.js` |
| 후보 화면 | `src/pages/HomePage.jsx`, `src/pages/NewWantPage.jsx`, `src/pages/WantDetailPage.jsx`, `src/components/WantForm.jsx` |
| 보유 화면 | `src/pages/OwnsPage.jsx`, `src/components/OwnManageDialog.jsx`, `src/components/WantCard.jsx` |
| 비교·분류·가격 | `src/lib/match.js`, `src/lib/constants.js`, `src/lib/price.js` |
| 데이터 조회·변경 | `src/hooks/useWardrobeData.jsx`, `src/hooks/useWants.js`, `src/hooks/useOwns.js` |
| API·검증·인증 | `server/routes/auth.js`, `server/routes/wants.js`, `server/routes/owns.js`, `server/lib/items.js`, `server/lib/jwt.js` |
| 공통 API 동작 | `server/app.js`, `server/middleware/rate-limit.js`, `server/middleware/error-handler.js` |
| 데이터 관계 | `prisma/schema.prisma` |

기능을 변경할 때 해당 기능 ID, 입력 규칙, 상태·삭제 정책, API 요약, 인수 확인 시나리오를 함께 갱신한다.
