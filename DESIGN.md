# 디자인 시스템 (있니 / Inni)

## 0. 문서 정보

| 항목 | 내용 |
|------|------|
| 프로젝트명 | 있니 (Inni) |
| 문서 버전 | v1.2 |
| 작성일 | 2026-08-19 |
| 기준 PRD | `PRD.md` v1.4 |
| 미리보기 | `design-system.html` (가이드), `app-preview.html` (앱 셸) |
| UI | Tailwind CSS + shadcn/ui |

**한 줄:** Quiet Pause. 종이 메모 + 체크리스트. 색은 거의 안 쓰고, 겹침 경고만 눈에 띄게.

---

## 1. 시각 원칙

있니는 옷장이 아니라 **결제 직전의 10초**다. 무신사·에이블리처럼 사진이 많은 **앱 화면**의 반대편에 선다. 랜딩 2시안만 소개용 라이프스타일 사진을 쓴다.

| 한다 | 하지 않는다 |
|------|-------------|
| 앱 화면은 한 화면에 할 일 하나 | 룩북, 코디 피드, 옷장 사진 그리드 |
| 앱에서 옷은 글과 스와치 | 후보·보유에 상품 사진 업로드 |
| 랜딩은 `src/assets` 사진 6구역 | 미션 5 히어로, 사진으로 옷장 채우기 |
| 겹침 Alert가 앱의 유일한 드라마 | 글래스모피즘, 네온, 다크 퍼스트 |
| 여백으로 속도를 늦춘다 | 옷장 완성도 게이지, 온보딩 “옷장 채우기” |
| shadcn 변형으로 통일 | 미션 5 시안 레이아웃 재사용 |
| 어떤 기기든 390px 앱 폭 | PC 와이드 그리드, `md:` 2열, 사이드바 |

**분위기 키워드:** 따뜻함, 짧은 확인, 숙제 아님, 장바구니 아님.

구현 시 커스텀 CSS를 늘리지 않는다. 아래 토큰을 shadcn `index.css`에 넣고, 컴포넌트는 `variant`로만 나눈다.

---

## 2. 컬러

배경은 순백이 아니라 **따뜻한 종이**. Primary(테라코타)는 CTA와 겹침에만 쓴다.

### 2.1 시맨틱 토큰 (shadcn HSL)

라이트만 사용한다. MVP에서 `dark` 클래스를 켜지 않는다.

```css
:root {
  --background: 38 33% 95%;       /* #F6F3EE 종이 */
  --foreground: 24 10% 10%;       /* #1C1917 잉크 */
  --card: 36 40% 98%;             /* #FAF8F4 카드 */
  --card-foreground: 24 10% 10%;
  --popover: 36 40% 98%;
  --popover-foreground: 24 10% 10%;
  --primary: 20 68% 46%;          /* #C45C26 테라코타 */
  --primary-foreground: 36 40% 98%;
  --secondary: 36 18% 92%;        /* #EFEBE4 */
  --secondary-foreground: 24 10% 16%;
  --muted: 36 18% 92%;
  --muted-foreground: 25 8% 42%;  /* 보조 글 */
  --accent: 22 45% 92%;           /* 겹침 Alert 배경 */
  --accent-foreground: 20 68% 32%;
  --destructive: 0 55% 42%;       /* 삭제·검증 실패. 네온 레드 금지 */
  --destructive-foreground: 0 0% 100%;
  --border: 32 14% 86%;
  --input: 32 14% 86%;
  --ring: 20 68% 46%;
  --radius: 0.625rem;            /* 10px */
}
```

| 역할 | 토큰 | 쓰는 곳 |
|------|------|---------|
| 페이지 배경 | `background` | `body`, 레이아웃 |
| 본문 | `foreground` | 제목, 카드 제목, 입력값 |
| 보조 | `muted-foreground` | 힌트, 날짜, 가격, 고지 |
| CTA | `primary` | 후보 추가, 저장, 로그인, 안 샀다 |
| 겹침 | `accent` + `primary` 보더 | “집에 비슷한 옷 n개” Alert |
| 위험 | `destructive` | 필드 에러, 삭제 확인 |
| 선 | `border` | 카드, 인풋, 탭 밑줄 |

그림자는 거의 쓰지 않는다. 카드는 `shadow-none` + `border`. 모달은 shadcn 기본 오버레이만.

### 2.2 옷 색 스와치

옷 사진 대신 **12~16px 원**. 흰 원은 보더가 있어야 종이 배경에서 보인다.

| 값 | 라벨 | hex | 노트 |
|----|------|-----|------|
| `black` | 블랙 | `#1C1917` | |
| `white` | 화이트 | `#FAFAF8` | `border: 1px solid hsl(var(--border))` |
| `gray` | 그레이 | `#8A8580` | |
| `navy` | 네이비 | `#1E3A5F` | |
| `beige` | 베이지 | `#D4C4A8` | |
| `brown` | 브라운 | `#C4B5A0` | 대각 스트라이프 optional |
| `other` | 기타 | `#C4B5A0` | 무지개 콘 또는 스트라이프. 라벨은 `colorDetail` |

스와치 옆에 한글 라벨을 항상 붙인다. 색맹·작은 원만으로는 구분하지 못하게 하지 않는다.

---

## 3. 타이포그래피

**폰트:** Pretendard. 시스템 산세리프 폴백.

```css
--font-sans: "Pretendard", "Apple SD Gothic Neo", "Noto Sans KR", sans-serif;
```

패션 앱처럼 얇은 영문 세리프, 올캡스 로고, 자간을 넓힌 영문 디스플레이는 쓰지 않는다.

| 역할 | 크기 / 두께 / 줄간격 | 예시 |
|------|----------------------|------|
| 앱 이름 | 20px / 600 / 1.3 | 헤더 “있니” |
| 화면 제목 | 24px / 600 / 1.3 | “살까 싶은 옷” |
| 카드 제목 | 16px / 600 / 1.4 | “블랙 니트” |
| 본문 | 15px / 400 / 1.6 | 설명, 폼 라벨 값 |
| 보조 | 13px / 400 / 1.5 | 날짜, 고지, empty 본문 |
| 배지 | 12px / 500 / 1 | 진행 중, 상의 |

한글 제목은 한 줄이 기본. 카드 제목이 길면 `truncate`.

---

## 4. 레이아웃

처음부터 **모바일 웹만** 만든다. PC로 열어도 레이아웃이 넓어지지 않는다. 반응형 대시보드가 아니라, 가운데 세운 폰 폭 앱이다.

### 4.1 앱 셸

| 항목 | 값 |
|------|-----|
| 앱 폭 | `390px` (`--app-width`). 그 이하면 `100%` |
| 앱 높이 | `100dvh` (주소창 변동 포함) |
| 앱 배경 | `--background` 종이 |
| PC 바깥 | 앱보다 한 단계 어두운 따뜻한 회색. 가운데 정렬 |
| 브레이크포인트 | **없음.** `sm:` / `md:` / `lg:`로 레이아웃을 바꾸지 않는다 |

```
PC 뷰포트
┌──────────────────────────────────┐
│  desk (바깥, 클릭 없음)           │
│         ┌──────────┐             │
│         │  390px   │  ← 앱만 UI  │
│         │  100dvh  │             │
│         └──────────┘             │
└──────────────────────────────────┘
```

바깥은 장식이 아니다. 앱이 아닌 영역에는 카피·배너·2열 콘텐츠를 두지 않는다. 노치 폰 프레임(베젤, 홈 인디케이터)도 그리지 않는다.

```css
:root { --app-width: 390px; }

html, body { height: 100%; }
body {
  background: hsl(32 14% 82%); /* desk. 앱 밖 */
}

#root {
  width: 100%;
  max-width: var(--app-width);
  min-height: 100dvh;
  margin-inline: auto;
  background: hsl(var(--background));
  display: flex;
  flex-direction: column;
}

@media (min-width: 391px) {
  #root {
    border-inline: 1px solid hsl(var(--border));
  }
}
```

Tailwind로는 `max-w-[390px] mx-auto min-h-dvh` 래퍼 하나. 그 안의 페이지는 전부 `w-full`.

### 4.2 셸 안

- **페이지 패딩:** 좌우 20px, 본문 상하 20px. 헤더 아래 16px.
- **카드 간격:** `flex flex-col gap-3`.
- **헤더:** sticky, 로고 왼쪽, 보유·로그아웃 텍스트 링크 오른쪽. 하단 보더만. 높이 56px. 셸 너비를 넘지 않음.
- **하단 고정 CTA:** 홈의 “후보 추가”만. `sticky bottom`은 셸 안(390px)에만.
- **상세 액션:** 샀다 / 안 샀다는 항상 한 줄 2버튼 (`flex gap-2`). 폭이 고정이라 `sm:flex-row` 분기가 필요 없다.
- **모달·토스트:** 뷰포트 전체가 아니라 `#root`(390px) 기준. Dialog overlay도 셸 안에.

### 4.3 viewport

```html
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
```

헤더·하단 CTA는 `env(safe-area-inset-*)`를 패딩에 더한다. 가로 모드는 지원하지 않는다(세로 기준).

---

## 5. 컴포넌트 규칙

shadcn을 디자인 시스템으로 쓴다. 새 버튼 컴포넌트를 만들지 않는다.

### 5.1 Button

| 용도 | variant | 화면당 |
|------|---------|--------|
| 후보 추가, 저장, 로그인, 가입 | `default` (primary) | 앱 CTA 1개. 랜딩은 구역마다 가입 버튼 가능 |
| 샀다 | `outline` | 상세 |
| 안 샀다 | `default` | 상세. 제품의 성공 행동 |
| 집에 이런 거 있음 | `secondary` | 상세 |
| 삭제 | `ghost` + destructive 텍스트 | 상세 |
| 로그아웃 | `ghost` size `sm` | 헤더 |

로딩: 버튼 `disabled` + shadcn `Spinner`. `isLoading` prop을 버튼에 추가하지 않는다.

안 샀다가 primary인 이유: 있니의 성공은 구매가 아니라 **안 산 기록**이다. 샀다는 보조.

### 5.2 Card

Want 한 줄 = `Card`. 왼쪽 스와치 + 제목 + 카테고리 Badge + 상태 Badge. 가격이 있으면 `muted-foreground`로 오른쪽.

호버는 보더가 조금 진해지는 정도. 리프트 섀도우 없음. 클릭 영역은 카드 전체(상세로).

### 5.3 Badge

| 상태 | 스타일 |
|------|--------|
| `pending` 진행 중 | outline, foreground |
| `bought` 샀다 | secondary, muted |
| `skipped` 안 샀다 | primary 연한 배경 (`accent`) + primary 글 |
| 카테고리 | secondary |

### 5.4 Alert

| 상황 | variant | 카피 |
|------|---------|------|
| 겹침 | 커스텀: `bg-accent border-l-4 border-l-primary` | “집에 비슷한 옷 n개” + 보유 제목 나열 |
| 실습 로그인 | default | “서버 없는 실습 로그인입니다. 비밀번호는 이 브라우저에만 남습니다.” |
| 저장 실패 | destructive | “저장하지 못했습니다. 다시 시도해 주세요.” |
| 검증 | 필드 `FormMessage`. Alert로 올리지 않음 |

겹침 Alert는 상세·작성 미리보기 **최상단**. 접지 않는다.

### 5.5 Form

필수: 이름, 카테고리, 색. 선택: 링크, 가격, 메모.

- 라벨은 15px, 필수만 이름 옆에 표시하지 않고 카피로 한 줄: “이름, 카테고리, 색만 있으면 됩니다.”
- 카테고리·색은 `Select`. 색 옵션 앞에 스와치.
- **기타:** 카테고리 `etc` → 인풋 placeholder `종류를 입력하세요` (`categoryDetail`). 색 `other` → 색 이름 (`colorDetail`). 비우면 필드 에러.
- 에러: 인풋 `aria-invalid`, 메시지 “이름을 적어 주세요”처럼 필드 단위.

### 5.6 Empty

아이콘은 작게 하나만. 큰 일러스트 없음.

| 화면 | 제목 | 본문 | 행동 |
|------|------|------|------|
| 홈 | 살까 싶은 옷을 넣어 보세요 | 지금 장바구니에 있는 것 1개면 됩니다. | 후보 추가 |
| 보유 | 확인할 때 한 줄만 추가하면 됩니다 | 옷장 전체를 올리지 않아도 됩니다. | 없음 (홈으로 유도 링크만) |
| 필터 결과 | 이 조건의 옷이 없습니다 | 필터를 바꿔 보세요. | 필터 해제 |

### 5.7 Tabs / 필터

홈: `진행 중 | 샀다 | 안 샀다`. 기본 `진행 중`.

카테고리 필터는 Tabs 아래 **칩**. 전체 + 상의·하의·아우터·신발·가방·기타. 다중 선택 아님.

### 5.8 Toast / Dialog

- 저장 실패, 삭제 완료: sonner 또는 `Alert`. 성공 토스트는 남발하지 않음. 목록 변화로 충분.
- 삭제: `AlertDialog`. 제목 “이 후보를 지울까요?”, 확인은 destructive.

---

## 6. 화면별 레이아웃

미션 5와 분리. 톤은 짧은 확인. 모든 화면은 4.1 셸 안에서만 그린다. PC용 별도 레이아웃 없음.

### `/` 랜딩 (비로그인, 2시안)

헤더: 있니 + 로그인. 본문은 세로 스크롤 6구역. 사진은 `src/assets` (`1hero src.png` … `6cta src.png`). 앱 목록처럼 옷 그리드를 만들지 않는다.

1. 히어로 풀블리드 사진 + 제목 + CTA “살까 싶은 옷 1개 넣어 보기”
2. 10초면 됩니다 — 사진 + 이름 / 카테고리 / 색
3. 겹침 목업 — 검은 니트 카드 + 알림(가로는 카드와 같음)
4. 안 사면 그게 성공 — 사진 위 샀다/안 샀다 목업
5. 확인은 무료 — 무료 0원 카드, Pro 한 줄, CTA “무료로 확인하기”
6. 필요한 옷만 남겨 보세요 — 사진 + CTA

버튼 아래 자물쇠·“무료로 확인” 문구 없음. 1시안 글 랜딩은 `/v1`.

### `/login`

셸 세로 중앙. 앱 이름 “있니”, 부제 “집에 비슷한 옷 있니?”. 그 아래 실습 로그인 Alert. 이메일·비밀번호. 기본은 로그인, 보조 링크가 회원가입.

소셜 버튼, 큰 로고 마크, 풀블리드 이미지 없음. 로그인 카드를 `max-w-sm`으로 또 줄이지 않는다. 이미 390px다.

### `/` 홈 (로그인)

헤더 → 제목 “살까” → 상태 Tabs → 카테고리 칩 → 카드 리스트 또는 Empty → 하단 “후보 추가”.

카드에 겹침이 있으면 작은 점(테라코타) + “집에 있음” 보조 텍스트. 상세로 들어가기 전에 힌트만.

### `/wants/new`

제목 “살까 싶은 옷”. 폼. 카테고리 기타면 종류 입력, 색 기타면 색 이름. 카테고리+색(기타면 상세까지)이 채워지면 같은 조건 보유가 있을 때 겹침 미리보기 Alert. 제출 “넣기”. 취소는 ghost.

### `/wants/:id`

1. 겹침 Alert (있으면 최상단)
2. 제목 + 스와치 + 카테고리 + 가격/링크
3. “집에 이런 거 있음” — 짧은 인풋 + secondary 버튼. 한 줄 추가가 기본 경로
4. 샀다 / 안 샀다
5. 삭제 (맨 아래, 약한 스타일)

`pending`이 아니면 샀다/안 샀다/보유 추가를 숨기고 상태 Badge만.

### `/owns`

제목 “집에 있는 것”. 짧은 리스트. 스와치 + 제목 + 카테고리(기타면 적은 종류) + 출처(`샀을 때` / `직접 적음`). 길게 누르면 수정. 게이지 없음.

---

## 7. 카피 톤

짧고 구어체. “등록”, “아이템”, “ Closet” 같은 관리 말 대신 **살까 / 집에 / 있니**.

| 상황 | 쓴다 | 안 쓴다 |
|------|------|---------|
| empty | 살까 싶은 옷을 넣어 보세요 | 아직 아이템이 없습니다 |
| 겹침 | 집에 비슷한 옷 2개 | 중복 감지됨 |
| 성공 | 안 샀다로 남겼어요 | 스킵 완료 |
| 보유 추가 | 집에 이런 거 있음 | 보유 의류 등록 |
| 로그인 고지 | 실습용이에요. 서버는 없습니다 | Warning: localStorage auth |

---

## 8. 모션

거의 없음. 로딩 스피너, AlertDialog 페이드만. 리스트 스태거, 페이지 슬라이드, 하트 애니메이션 없음.

---

## 9. 접근성

- 대비율: 본문 `foreground` on `background` 통과. muted는 13px 이상에서만.
- 겹침을 색만으로 알리지 않음. 문구 필수.
- 스와치에 `title` 또는 옆 라벨.
- 포커스 링은 `--ring` (테라코타). 제거하지 않음.
- 삭제 다이얼로그에 `AlertDialogTitle` 필수.

---

## 10. Tailwind / shadcn 적용 순서

1. `index.css`에 2.1 토큰을 넣는다. 기본 zinc/blue를 덮어쓴다.
2. `radius` 10px.
3. 쓸 컴포넌트만 추가: `Button`, `Card`, `Input`, `Select`, `Badge`, `Alert`, `AlertDialog`, `Tabs`, `Form`(또는 Field), `Sonner`, `Empty`, `Spinner`.
4. `#root`를 `max-w-[390px] mx-auto min-h-dvh`로 감싼다. 페이지·모달은 이 안에서만.
5. `sm:` `md:` `lg:`로 그리드·폰트·패딩을 키우지 않는다.
6. `design-system.html` / `app-preview.html`과 토큰·폭이 다르면 코드가 아니라 미리보기를 맞추지 말고 CSS를 맞춘다.

---

## 11. 하지 말 것 (체크)

- [ ] 후보·보유 상품 이미지 그리드
- [ ] 옷장 완성도 / 프로그레스 바
- [ ] 미션 5 랜딩 히어로
- [ ] 다크 모드 우선
- [ ] 앱 화면에서 primary 버튼 한 화면에 2개 이상 (상세의 안 샀다 + 저장은 화면이 다름. 랜딩 가입 CTA는 구역마다 가능)
- [ ] 장바구니 아이콘을 로고로
- [ ] 가격을 가장 크게
- [ ] 커스텀 그라디언트 배경
- [ ] PC에서 2열·사이드바·`max-w-5xl`
- [ ] `sm:` / `md:`로 레이아웃이 바뀌는 분기
- [ ] 앱 밖에 랜딩 카피나 폰 베젤 장식

---

## 12. 결정 요약

- 스타일: **Quiet Pause**
- 종이 배경 + 잉크 텍스트 + 테라코타 하나
- 안 샀다 = primary, 샀다 = outline
- 겹침 Alert = 왼쪽 primary 보더
- 옷은 앱에서 스와치로. 랜딩 2시안만 소개 사진
- 앱 폭 390px. PC는 가운데 컬럼만. 반응형 분기 없음
- shadcn 변형만
