# 회원 등급과 체험 정책

결제 없이 계정의 `FREE` / `PAID` 등급으로 권한을 구분한다. 이메일·구글 로그인 모두 같은 정책이다. 신규 가입은 FREE이며 공개 API에서 등급·사용 횟수를 변경할 수 없다.

| 구분 | 일반 FREE | 유료 PAID |
|---|---|---|
| 신규 저장 | 계정당 누적 50회 | 제한 없음 |
| 보관 | 각 항목 저장 시각부터 30×24시간 | 만료 없음 |
| 삭제·만료 시 횟수 복구 | 없음 | 해당 없음 |
| 월별 초기화 | 없음 | 해당 없음 |

- 구매 후보 신규 저장, 내 옷장 직접 등록은 각각 1회 사용한다. 구매 완료 전환·수정·삭제는 횟수를 바꾸지 않는다.
- 구매 완료로 생성된 Own은 Want의 만료일을 물려받는다. 수정·상태 변경으로 기간을 연장하지 않는다.
- 50회 소진 시 신규 저장만 403 `TRIAL_SAVE_LIMIT_REACHED`로 거절한다. 남은 항목의 조회·수정·구매 완료·삭제는 가능하다.
- 사용자 행 잠금과 트랜잭션으로 동시 저장 한도를 지킨다. 저장 실패 시 카운터도 롤백한다.
- `POST /api/wants`, `POST /api/owns`의 `Idempotency-Key`에 같은 계정·키·내용을 재전송하면 200으로 기존 항목을 반환하며 중복 차감하지 않는다. 신규 생성은 201이다. 다른 내용으로 같은 키를 재사용하거나 삭제·만료된 요청을 재시도하면 409이다. 헤더 생략 시 매 요청은 새 저장이다.
- 프론트엔드는 네트워크/서버 오류 후 현재 페이지 세션 내에서 같은 내용의 요청 키를 재사용한다. 새로고침 이후까지 미완료 요청을 보존하지는 않는다.
- 요청 기록은 삭제 이후에도 유지한다. 항목 원문은 저장하지 않고 키·해시·항목 ID만 남기며 회원 탈퇴 시 함께 삭제한다.
- 로그인·`GET /api/auth/me`의 `user.membership`, 신규 저장 응답의 `membership`에 등급·누적 사용·남은 횟수·저장 가능 여부를 포함한다. 항목의 `expiresAt`은 만료 시각 또는 null이다.

## 등급 변경 (서버 전용)

마이그레이션 적용 후, 올바른 환경의 `.env`가 설정된 신뢰할 수 있는 터미널에서 실행한다. 아래 명령은 연결된 DB의 실제 계정을 변경한다.

```powershell
npm run membership:set -- member@example.com PAID
npm run membership:set -- member@example.com FREE
```

- PAID 전환: 이미 만료된 항목은 정리하고 남은 항목의 만료일을 해제한다. 삭제된 데이터는 복원하지 않는다.
- FREE 복귀: 기존 체험 사용 횟수를 유지하고 남은 항목에 전환일부터 30일을 부여한다.
- 같은 등급을 반복 지정해도 보관 기간을 연장하지 않는다.
- 결제가 없으므로 PAID에는 자동 종료일이 없고 운영자가 FREE로 변경하기 전까지 유지된다.
- DB의 `plan`만 직접 수정하지 않는다. 위 명령이 만료일·등급 변경을 하나의 트랜잭션으로 수행한다.

## 만료 정리

API는 만료 시각부터 모든 목록 페이지·상세·수정·구매 완료에서 항목을 제외한다. 삭제 작업이 늦어져도 접근하거나 유료 전환으로 되살릴 수 없다. 화면은 30초마다 만료를 반영하고 창 포커스 시 회원 정보와 목록을 갱신한다.

- Vercel Cron은 매일 UTC 18시(KST 다음 날 03시)에 `/api/cron/cleanup-expired`를 호출하도록 설정했다. 실제 실행 시각에는 플랫폼 지연이 있을 수 있다.
- 서버 환경에 독립적인 32자 이상 난수 `CRON_SECRET`이 필요하다. 일반 로그인 토큰으로 호출할 수 없고 미설정 시 503으로 삭제를 거부한다.
- 1회 최대 100명을 처리한다. 20초가 지나면 새 회원 처리를 멈추고 진행 중 트랜잭션만 마친다. 남은 데이터는 `hasMore: true`와 서버 경고로 표시하며 다음 실행이나 수동 작업으로 정리한다.
- 연결된 Own을 먼저, Want를 다음에 삭제한다. 구매 완료·등급 변경과 같은 사용자 행 잠금을 사용하고 횟수는 차감하지 않는다.
- 수동 전체 정리 명령: `npm run items:cleanup`.
- Cron은 운영 배포 후 작동한다. 코드 작성만으로 운영 환경을 변경하지 않는다. [Vercel Cron 인증 문서](https://vercel.com/docs/cron-jobs/manage-cron-jobs)

## 기존 데이터 이전과 운영 적용

운영 DB 마이그레이션과 배포는 별도 작업이다. GitHub 업로드만으로 운영 적용이 완료되는 것은 아니다. 새 DB 필드가 없는 상태에서 새 코드가 자동 배포되지 않도록 작업 브랜치에서 검토한다. 실제 전환 시에는 저장 요청을 잠시 중단하고 마이그레이션·배포를 연속으로 진행한다. 구버전 서버가 마이그레이션 후에도 저장을 받으면 새 카운터와 만료일을 기록하지 못한다.

1. `20260918090000_membership` 마이그레이션을 검토한 뒤 `npm run db:deploy`로 적용한다. 이 명령은 연결 대상 DB를 변경한다.
2. 기존 회원은 FREE로 시작한다. 남아 있는 Want 수 + 연결되지 않은 Own 수로 누적 사용 횟수를 초기화한다. 과거 삭제된 기록은 알 수 없어 포함하지 않는다. 이미 50회를 넘었어도 기존 항목을 즉시 삭제하지 않는다.
3. 기존 항목에는 마이그레이션 실행일부터 30일을 부여한다. 신규 항목은 각 저장일부터 30일이다.
4. `CRON_SECRET` 설정 후 새 코드를 배포한다. 마이그레이션 전 새 코드를 실행하면 새 DB 필드가 없어 실패한다.
5. 필요한 계정에만 서버 명령으로 PAID를 부여한다. 결제 버튼·결제 연동·공개 등급 변경 API는 없다.

## 검증

`npm test`의 통합 테스트는 PGlite 기반 일회용 PostgreSQL과 Prisma를 사용하며 `.env` 또는 운영 DB에 연결하지 않는다. 마이그레이션, RLS 활성화, 동시 저장, 재시도, 실패 롤백, 구매 전환, 만료, 등급 변경, 다른 회원 접근 방지, Cron 인증을 검증한다. PGlite 다중 연결은 트랜잭션을 직렬화하므로 실제 PostgreSQL 부하·잠금 경합 성능 검증을 대체하지 않는다.

변경 후 `npm test`, `npm run lint`, `npm run build`, `npm run db:validate`를 실행한다.
