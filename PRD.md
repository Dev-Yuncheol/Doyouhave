# 서비스 기획서 (PRD)

## 0. 문서 정보

| 항목 | 내용 |
|------|------|
| 프로젝트명 | 있니 (Inni) |
| 문서 버전 | v1.4 |
| 작성일 | 2026-08-19 |
| 수정일 | 2026-08-20 |
| 서비스 형태 | React 웹앱 (프론트엔드 MVP, 백엔드 없음) |
| 개발 단계 | 스프린트 미션 6 |
| 목표 | 옷을 사기 전에 “집에 있니?”를 확인하고, 샀다/안 샀다를 남겨 비슷한 옷을 또 사는 실수를 줄인다 |

**이번 문서에서 확정한 결정**

| # | 질문 | 결정 |
|---|------|------|
| 1 | 보유 옷을 어떻게 쌓나 | B. 살까 목록 + 아주 짧은 보유 목록. 확인할 때 “집에 검은 니트 있음”만 추가 |
| 2 | 비슷함 기준 | A. 같은 카테고리 + 색. 기타면 `categoryDetail` / `colorDetail`까지 같아야 함 |
| 3 | 로컬 로그인 | A. 이번 제출에 포함 (이메일·비밀번호 흉내, localStorage) |
| 4 | 미션 5 시안 | 재사용하지 않음. 새 화면 |
| 5 | 저장 | A. localStorage만 |
| 6 | 이번 주 P1 | empty / 에러 / 로딩 |
| 10 | 스타일 | Tailwind CSS + shadcn/ui |
| 11 | 수익 모델 | 핵심 루프는 무료. Pro는 아낀 금액·통계. 월/연/평생. 체험 14일. 등록 개수로 잠그지 않음 |
| 12 | 랜딩 | 비로그인 `/`는 2시안(사진 6구역). 1시안은 `/v1` 백업. CTA는 회원가입. 미션 5 시안 재사용 안 함 |

---

# 1. 서비스 개요

## 1.1 한 줄 설명

결제 직전에 **집에 비슷한 옷이 있는지** 보는 체크리스트. 디지털 옷장이 아니다.

## 1.2 핵심 컨셉

- 장바구니 = 이 가게에서 지금 살 것
- 있니 = 가게와 상관없이, **집에 이미 있는 것**과 겹치는지
- 옷장 전체를 사진으로 만들지 않는다. **살 뻔한 옷**과 **그때 떠오른 보유 옷**만 적는다
- 첫 후보를 넣은 뒤 바로 쓸모가 보여야 한다. 온보딩에 “옷장 채우기”가 없다

## 1.3 타겟

- 1차: 무신사·에이블리·쿠팡 등으로 옷을 자주 사고, 옷이 한눈에 안 보이는 사람
- 비타깃: 옷이 적고 옷장이 한눈에 보이는 사람, 옷장 전체를 디지털화하고 싶은 헤비유저

## 1.4 제품 정체성

- 숙제가 아니라 **사기 직전의 10초**
- 코디 추천·AI 스타일리스트가 아님
- “저장함”이 아니라 **다음번 같은 구매를 막는 앱**
- 시각은 미션 5와 분리한다. 있니 전용 화면으로 새로 만든다

---

# 2. 핵심 가설

## 2.1 문제 가설

온라인에서 옷을 볼 때 사람들은 “없는 것 같은데”로 결제한다. 장바구니·위시·주문내역은 **집에 있는 옷**을 모아서 보여주지 못한다. 그 결과 비슷한 옷을 또 산다.

## 2.2 솔루션 가설

사고 싶은 옷을 카테고리·색과 함께 적고, 같은 조건의 보유 옷이 있으면 결제 전에 보여 주면, 일부 구매를 **안 샀다**로 돌릴 수 있다. 전체 옷장 등록은 시키지 않아도 이 루프는 돈다.

보유는 미리 만들지 않는다. 후보를 확인할 때 “집에 이런 거 있음” 한 줄을 남기거나, **샀다**로 표시한 옷만 보유로 쌓인다.

## 2.3 MVP 검증 질문

1. 옷을 살까 말까 할 때, 이 앱을 열어 후보를 적는가?
2. 겹침 의심이 뜨면 실제로 구매를 멈추는가?
3. 전체 옷장을 채우지 않아도, 후보 몇 개만으로 쓸모가 느껴지는가?

---

# 3. 사용자 유형과 권한

## 3.1 사용자 유형(MVP)

| 유형 | 설명 | 권한 |
|------|------|------|
| 비로그인 | 세션 없음 | 랜딩(`/`), 로그인·회원가입 |
| 로컬 사용자 | 이메일·비밀번호를 localStorage에 저장한 가짜 계정 | 본인 `userId`의 후보·보유만 CRUD, 로그아웃 |

실제 인증 서버·소셜 로그인은 없다. 비밀번호는 실습용으로 브라우저에 저장되며, 보안 로그인이 아님을 UI에 짧게 고지한다.

## 3.2 향후 권한

미션 7 이후: 서버 계정, 기기 간 동기화, 쇼핑몰 확장 프로그램. MVP 밖.

---

# 4. MVP 기능 범위

## 4.1 P0 기능 (이번 제출)

| ID | 기능 | 설명 |
|----|------|------|
| F1 | 로컬 로그인 | 회원가입·로그인·로그아웃. 세션은 localStorage. 보호 라우트 |
| F2 | 살까 후보 등록 | 이름*, 카테고리*, 색*, 링크·가격은 선택. 카테고리·색 **기타**는 직접 입력. 필수값 없으면 에러 메시지 |
| F3 | 보유 최소 등록 | 후보 확인 중 “집에 이런 거 있음”으로 Own 1건 추가. 또는 샀다 → Own 생성. 보유 수정도 기타 직접 입력 |
| F4 | 겹침 표시 | 같은 `category` + `color`인 Own이 있으면 “집에 비슷한 옷 n개”. 기타는 적은 종류·색 이름이 같을 때만 |
| F5 | 샀다 / 안 샀다 | `bought`면 Own 생성(`source: 'bought'`). `skipped`는 성공으로 취급 |
| F6 | 목록·상세·필터 | 진행중/샀다/안 샀다, 카테고리 필터. React Router |
| F7 | empty / 에러 / 로딩 | 빈 목록, 검증 실패, 저장 실패, 짧은 로딩 상태 |
| F8 | 랜딩 | 비로그인 `/` = 2시안. `src/assets` 사진 6구역. CTA → 회원가입. 1시안은 `/v1` |

## 4.2 P1 기능 (이번 제출 제외, 다음)

- 이번 달 안 산 횟수·아낀 금액 요약 → **Pro 잠금 기능** (16절)
- 이미지 업로드
- 24시간 강제 대기 → Pro

## 4.3 MVP 제외 기능

- 옷장 전체 사진 카탈로그, AI 코디, 배경 제거
- 쇼핑몰 장바구니 연동, 브라우저 확장
- 실제 API·DB·소셜 로그인
- 결제·구독 UI (수익 모델은 16절, 구현은 미션 7 이후)
- 미션 5 랜딩/시안 레이아웃 재사용 (있니 전용 랜딩은 F8, 17절)

---

# 5. 사용자 흐름

```
비로그인 `/` 랜딩
  → CTA “살까 싶은 옷 1개 넣어 보기” → `/login?mode=signup`
회원가입 또는 로그인
  → 홈: 진행 중 후보 목록
       (없으면 empty: “살까 싶은 옷을 넣어 보세요”)
  → 후보 추가
       → 이름·카테고리·색 입력. 기타면 종류/색 이름 (실패 시 필드 에러)
       → 저장 중 로딩
       → 같은 카테고리+색(기타면 상세까지) 보유가 있으면 겹침 미리보기
  → 상세
       → “집에 이런 거 있음” → 보유 1개 추가 → 겹침 갱신
       → 샀다 → 보유로 이동 → 목록
       → 안 샀다 → 목록에서 안 샀다 상태
  → 보유 목록: 짧은 리스트, 카테고리/색 필터
  → 로그아웃
```

**라우트**

| 경로 | 화면 | 인증 |
|------|------|------|
| `/` | 랜딩 2시안 (비로그인) / 홈 (로그인) | 공개 + 분기 |
| `/v1` | 랜딩 1시안 백업 | 공개 |
| `/login` | 로그인·회원가입 | 공개 |
| `/wants/new` | 후보 작성 | 필요 |
| `/wants/:id` | 후보 상세 | 필요 |
| `/owns` | 보유 목록 | 필요 |

로그인 없이 보호 라우트에 가면 `/login`으로 보낸다. 랜딩 CTA는 `/login?mode=signup`.

---

# 6. 화면 설계

미션 5 시안과 분리한다. 있니 전용 화면을 새로 만든다. 톤은 결제 직전의 짧은 확인. 옷장 갤러리처럼 보이지 않게 한다.

| 화면 | 목적 | 핵심 UI (shadcn) |
|------|------|------------------|
| 랜딩 | 비로그인 소개. 2시안 사진 6구역. 옷장 카탈로그처럼 보이지 않게 | `Button`, `Card`, `Badge`, 겹침 `Alert`. CTA는 가입 |
| 로그인 | 가짜 계정 | `Card`, `Input`, `Button`, `Alert`(실습 로그인 고지) |
| 홈 | 지금 고민 중인 옷 | `Card` 리스트, `Badge` 상태, `Tabs` 또는 필터 칩, `Empty` |
| 후보 작성 | 10초 기록 | `Input`, `Select`(카테고리·색), 기타 시 `Input`, `Button`, 필드 `FormMessage` |
| 후보 상세 | 결제 전 확인 | 겹침 `Alert`, 보유 미리보기, 샀다/안 샀다 `Button`, 삭제 `AlertDialog` |
| 보유 | 집에 있는 것만 | 짧은 리스트. 옷장 완성도 게이지 없음. empty: “확인할 때 한 줄만 추가하면 됩니다” |
| 공통 | 대기·실패 | 버튼 `disabled`+스피너, 토스트 또는 `Alert` |

**카테고리:** 상의, 하의, 아우터, 신발, 가방, 기타  
**색:** 블랙, 화이트, 그레이, 네이비, 베이지, 브라운, 기타

필수 입력은 이름·카테고리·색 세 개. **기타**를 고르면 칸이 열린다. 카테고리 기타는 placeholder `종류를 입력하세요` (`categoryDetail`). 색 기타는 색 이름 (`colorDetail`). 비우면 필드 에러.

---

# 7. API 설계

미션 6: **HTTP API 없음.** 화면은 아래 함수만 부른다. 미션 7에서 같은 모양으로 서버를 붙인다.

```
getSession()
signUp({ email, password })
login({ email, password })
logout()

getWants({ status, category })
getWant(id)
createWant(payload)
updateWant(id, payload)
deleteWant(id)

getOwns({ category, color })
createOwn(payload)
updateOwn(id, payload)
deleteOwn(id)

findSimilarOwns({ category, color, categoryDetail, colorDetail })
  // category+color. etc/other면 상세 문자열까지
```

저장 실패 시 throw 또는 `{ ok: false, error }`. UI는 에러 메시지를 보여 준다. 인위적 짧은 delay로 로딩을 재현해도 된다 (200~400ms).

---

# 8. 데이터 설계

## 8.1 핵심 키 (localStorage)

| 키 | 내용 |
|----|------|
| `inni_users` | `{ id, email, password }[]` |
| `inni_session` | `{ userId }` 또는 없음 |
| `inni_wants` | Want[] |
| `inni_owns` | Own[] |

모든 목록은 `userId`로 걸러 읽는다.

## 8.2 ERD

```
User 1 ──< Want
User 1 ──< Own

Want.category + Want.color  ==  Own.category + Own.color
  (+ etc면 categoryDetail, other면 colorDetail)
  → 겹침 의심 (FK 없음, 조회 매칭)

Want.status = bought  →  Own 1건 생성 (source: bought, fromWantId)
후보 상세에서 수동 추가     →  Own 1건 생성 (source: manual)
```

## 8.3 스키마

```
User {
  id: string
  email: string
  password: string          // 실습용. 해시 없음. UI에 고지
  createdAt: string
}

Want {
  id: string
  userId: string
  title: string
  url?: string
  category: 'top' | 'bottom' | 'outer' | 'shoes' | 'bag' | 'etc'
  categoryDetail?: string   // category === 'etc'일 때만. 예: 모자
  color: 'black' | 'white' | 'gray' | 'navy' | 'beige' | 'brown' | 'other'
  colorDetail?: string      // color === 'other'일 때만. 예: 카키
  price?: number
  note?: string
  status: 'pending' | 'bought' | 'skipped'
  createdAt: string
  updatedAt: string
}

Own {
  id: string
  userId: string
  title: string
  category: Want['category']
  categoryDetail?: string
  color: Want['color']
  colorDetail?: string
  source: 'bought' | 'manual'
  fromWantId?: string
  createdAt: string
}
```

새로고침 후에도 목록이 유지된다.

---

# 9. 코드 아키텍처

```
src/
  pages/               라우트 화면 (조합만). LandingPageV2(`/`), LandingPage(`/v1` 백업), LoginPage, HomePage …
  components/ui/       shadcn 생성 컴포넌트
  components/          WantCard, WantForm, SimilarOwns, EmptyState, AuthGate, OwnManageDialog
  hooks/               useSession, useWants, useOwns
  lib/storage.js       localStorage 읽기/쓰기
  lib/match.js         category + color (+ etc/other 상세)
  lib/auth.js          signUp, login, logout
  assets/              랜딩 2시안 소스 사진 (`1hero src.png` … `6cta src.png`)
```

- 페이지는 데이터를 직접 localStorage에 쓰지 않는다. hook만 부른다.
- `WantForm`에 목록·필터·로그인까지 넣지 않는다.
- 겹침 로직은 `lib/match.js` 한곳.

---

# 10. 기술 스택

| 구분 | 선택 |
|------|------|
| UI | React |
| 라우팅 | React Router |
| 상태 | useState / useEffect. 전역 라이브러리 없음 |
| 저장 | localStorage |
| 스타일 | Tailwind CSS |
| 컴포넌트 | shadcn/ui (`Button`, `Card`, `Input`, `Select`, `Badge`, `Alert`, `AlertDialog`, `Tabs`, `Form` 등 필요한 것만) |
| 빌드 | Vite |
| 배포 | Vercel |
| 저장소 | GitHub Public |

shadcn은 디자인 시스템 역할을 한다. 버튼·폼·카드는 커스텀을 늘리지 않고 shadcn 변형으로 통일한다.

---

# 11. 환경변수

비밀키 없음.

```
VITE_APP_NAME=있니
```

`.env`에 계정·토큰을 넣지 않는다.

---

# 12. MVP 개발 순서

1. Vite + React + Tailwind + shadcn 초기화, 레이아웃·라우터
2. `auth` + 로그인/회원가입 + 보호 라우트
3. `storage` + Want CRUD + 홈/작성/상세
4. Own 수동 추가 + 샀다 시 Own 생성 + `match` 겹침 표시
5. 필터, 검증 에러, empty, 로딩
6. 비로그인 랜딩 (`/`)
7. 스타일 통일, Vercel 배포, GitHub push

---

# 13. MVP 성공 지표

**과제**

- [ ] 로그인 후 후보를 만들고, 목록·상세가 동작한다
- [ ] 같은 카테고리+색 보유가 있으면 상세에 겹침이 보인다 (기타는 적은 이름까지)
- [ ] 샀다/안 샀다가 반영되고, 새로고침 후에도 남는다
- [ ] 빈 목록 empty, 필수값 에러, 저장 중 로딩이 보인다
- [ ] GitHub Public + Vercel URL이 열린다
- [ ] 비로그인 `/`에서 랜딩이 보이고, CTA로 회원가입까지 간다

**제품 (있으면 좋음)**

- 후보 3개 이상 넣은 뒤에도 흐름이 이해된다
- `skipped`가 1개라도 생긴다

---

# 14. 리스크 및 대응

| 리스크 | 대응 |
|--------|------|
| 랜딩이 옷장 앱처럼 보임 | 앱 화면은 사진 그리드 없음. 랜딩 사진은 소개용. “디지털 옷장이 아니다”를 히어로에 적는다 |
| 보유가 없어 겹침이 안 뜸 | 상세에 “집에 이런 거 있음” 한 줄 추가를 기본 경로로 둔다 |
| 장바구니와 같게 보임 | 카피: “이 쇼핑몰이 아니라 집” |
| 필드를 늘려 숙제가 됨 | 필수 입력 3개 (이름, 카테고리, 색) |
| 에이클로젯과 비교 | 코디/AI/사진 옷장을 하지 않는다고 명시. 요금도 등록 개수가 아니라 아낀 금액으로 잠근다 |
| 가짜 로그인으로 오해 | 로그인 화면에 실습용·서버 없음 고지 |
| 한 달 무료 + 싼 평생권 | 14일 체험. 평생권은 연의 3~4배. 핵심 확인은 무료로 남긴다 |
| 제휴 수수료가 “사라”를 유도 | 제휴는 샀다 확정 후에만. 안 샀다에는 붙이지 않음 |

---

# 15. 최종 결정 요약

- 이름: **있니**
- 범위: 사기 직전 체크. 디지털 옷장 아님
- 보유: 확인할 때 한 줄, 또는 산 옷만 쌓임
- 겹침: 카테고리 + 색. 기타는 적은 종류·색 이름까지
- 인증: 로컬 로그인 포함
- 화면: 미션 5와 별개. 비로그인 `/`는 랜딩 2시안. 1시안은 `/v1`
- 저장: localStorage
- UX 상태: empty / 에러 / 로딩
- UI: React + Tailwind + shadcn/ui
- 제출: GitHub Public + Vercel
- 수익: 핵심 루프 무료. Pro = 아낀 금액·통계. 월 2,900 / 연 19,900 / 평생 69,000. 체험 14일. 미션 6 구현 없음

---

# 16. 향후 수익 모델

미션 6 범위 밖. 결제·구독 UI는 만들지 않는다. 미션 7 이후 서버·동기화가 붙을 때 이 표를 따른다.

**하지 않는 것**

- 옷 등록 개수로 유료 전환 (에이클로젯형). 있니는 옷장을 채우지 않는다
- 첫 달 무료 후 한 번 결제하면 평생 무제한. 쇼핑은 띄엄띄엄이라 한 달 안에 루프가 안 돌고, 싼 평생권은 서버비를 남긴다
- 안 사기를 돕는 제품에, 사야만 수수료가 나는 제휴를 핵심 수익으로 둠

## 16.1 원칙

1. 후보 등록, 보유 한 줄, 겹침 확인, 샀다/안 샀다는 **계속 무료**. 진행 중 후보만 5개로 막는다 (옷장 한도가 아님)
2. 유료는 **성과**다. “이번에 안 사서 N원 아꼈다”
3. 체험은 **14일** Pro 전체. 한 달이 아니다. 끝나면 무료 티어로 내려간다. 계정을 잠그지 않는다
4. 가격은 월 / 연(주력) / 평생(연의 약 3.5배)
5. 제휴(무신사 큐레이터 등)는 확장 프로그램 이후, **샀다**에만 선택적으로

## 16.2 요금제

| 플랜 | 가격 | 대상 |
|------|------|------|
| 무료 | 0원, 기한 없음 | 결제 전 확인만 하는 사람 |
| Pro 체험 | 0원, **14일** | 가입 직후 1회. 카드 없이 시작 |
| Pro 월 | 2,900원/월 | 한 시즌만 쓰는 사람 |
| Pro 연 | **19,900원/년** (주력, 월 환산 약 1,660원) | 옷을 자주 사는 1차 타겟 |
| Pro 평생 | 69,000원, 1회 | 구독이 싫은 사람. 연의 약 3.5배 |

평생권은 현재 Pro 기능 + 이후 일반 업데이트. 별도 고비용 기능(사람 스타일리스트, 대량 AI)은 포함하지 않는다고 명시한다.

## 16.3 잠그는 것 / 안 잠그는 것

| 기능 | 무료 | Pro |
|------|------|-----|
| 회원가입·로그인 | ○ | ○ |
| 살까 후보 등록 | ○ 진행 중 5개 | ○ 무제한 |
| 보유 “집에 이런 거 있음” | ○ | ○ |
| 겹침 표시 (카테고리+색) | ○ | ○ |
| 샀다 / 안 샀다 | ○ | ○ |
| 목록·필터 | ○ | ○ |
| 이번 달 안 산 횟수 | 숨김 또는 “Pro” | ○ |
| 아낀 금액 합계 | 숨김 | ○ |
| 주/월 습관 통계 | — | ○ |
| 24시간 강제 대기 | — | ○ |
| 기기 간 동기화 | — | ○ (미션 7) |
| 이미지 업로드 | — | ○ |

진행 중 후보 5개는 유료 유도보다 **숙제처럼 쌓이지 않게** 하려는 제품 한도다. 샀다/안 샀다로 보낸 기록은 무료에서도 남는다.

체험 14일 동안은 표의 Pro 칸을 모두 연다. 끝나면 무료 칸만 남고, 이미 쌓인 안 샀다 기록은 지워지지 않는다. 합계 숫자만 잠긴다.

## 16.4 화면 카피 (초안)

**무료 홈, 통계 자리**

> 이번 달 안 산 횟수와 아낀 금액은 Pro에서 볼 수 있어요.

**페이월**

> 등록을 팔지 않습니다. 결제 전에 막은 결과를 보여 줍니다.
>
> 14일 동안 Pro 전체를 써 보세요. 끝나면 후보는 그대로 남습니다.

**연 강조**

> 연 19,900원 · 옷 한 벌 값을 한 번만 아껴도 본전입니다.

**평생**

> 69,000원, 한 번만. 구독 없음. Pro 기능과 이후 일반 업데이트가 포함됩니다.

**제휴 (확장 프로그램, 샀다 확정 후만)**

> 사기로 한 뒤에만 쇼핑몰로 이동합니다. 안 샀다에는 수수료가 없습니다.

## 16.5 나중에 붙여도 되는 것

- 크레딧: 이미지·AI처럼 서버비가 있는 기능만 건당
- 제휴 CPS: 샀다 링크에만. 안 샀다와 수익이 같은 방향을 보게
- 안 사기 챌린지: No-Buy 월, 스킵 N회. 배지·리더보드는 Pro

광고는 넣지 않는다. 10초 확인 화면에 배너는 거슬린다.

---

# 17. 랜딩 페이지

별도 문서 없음. 이 절이 전부다. 미션 5 시안을 쓰지 않는다. 앱과 같은 390px 프레임, Quiet Pause 톤.

**채택:** 2시안 (`LandingPageV2`). 비로그인 `/`. 사진 소스는 `src/assets`.  
**백업:** 1시안 (`LandingPage`)은 `/v1`. 글 위주, 요금 4줄 표.

**목적:** 비로그인이 “옷장 앱”이 아니라 “결제 전 10초 체크”로 이해한 뒤 가입한다.

**성공:** 랜딩만 보고 옷장 채우기를 기대하지 않는다. CTA 다음이 회원가입이다.

## 17.1 하지 않는 것

- 결제 버튼, 구독 체크아웃 (16절 구현은 미션 7). 요금 **표기만**
- AI 코디, 사진으로 옷장을 채우는 앱처럼 보이게 하기
- CTA를 가격·데모 영상으로 보냄

## 17.2 2시안 섹션 (`/`)

헤더: 있니 + 로그인. 버튼 아래 “무료로 확인 / 무료로 시작” 잠금 문구는 없음.

| # | 섹션 | 내용 |
|---|------|------|
| 1 | 히어로 | 니트 사진. 제목: 집에 비슷한 옷 있니? 부제: 결제 직전 10초 체크리스트. 디지털 옷장이 아닙니다. CTA: 살까 싶은 옷 1개 넣어 보기 |
| 2 | 10초 | 10초면 됩니다. 이름·카테고리·색. 사진 + 1 2 3 |
| 3 | 겹침 | 집에 비슷한 게 있으면 바로 보여줘요. 검은 니트 카드 + “집에 비슷한 옷 1개 / 작년 블랙 니트” (알림 가로는 카드와 같음) |
| 4 | 성공 | 안 사면 그게 성공입니다. 샀다 / 안 샀다 목업 |
| 5 | 요금 | 확인은 무료입니다. 무료 0원 카드(후보·겹침·샀다/안 샀다). Pro는 한 줄만. CTA: 무료로 확인하기. 월/연/평생 표 없음 |
| 6 | 닫기 | 필요한 옷만 남겨 보세요. CTA: 살까 싶은 옷 1개 넣어 보기 |

가입 CTA는 `/login?mode=signup`. 요금 카드에서 결제하지 않는다.
